aws_iam_instance_profile.ansible:
    attribute: "id"
    description: "ID for shared Ansible role"

aws_iam_instance_profile.eni-attach:
    attribute: "id"
    description: "ID for shared ENI attach role"

aws_iam_instance_profile.upena:
    attribute: "id"
    description: "ID for shared Upena role"
