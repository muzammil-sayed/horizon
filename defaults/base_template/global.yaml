aws_iam_instance_profile.boomi-node:
    attribute: "id"
    description: "ID for shared boomi-node role"

aws_iam_instance_profile.upena:
    attribute: "id"
    description: "ID for shared Upena role"
